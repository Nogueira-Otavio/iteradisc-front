import React from "react";
import Layout from "../../components/Layout/Layout";

function Venda() {
  return <Layout titulo="Vendas"><p style={{color:"var(--cor-creme)"}}>Em breve...</p></Layout>;
}
export default Venda;